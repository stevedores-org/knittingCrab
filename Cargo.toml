[package]
name = "knitting-crab"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2"
tokio = { version = "1", features = ["rt-multi-thread", "macros", "time", "sync", "process", "io-util"] }

[dev-dependencies]
